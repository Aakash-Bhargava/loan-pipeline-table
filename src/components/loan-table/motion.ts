/**
 * Durations that JavaScript needs to know about because it decides when an
 * element leaves the DOM. Each one mirrors a token in src/styles/tokens.css and
 * must be changed in both places together.
 */

/** --duration-exit */
export const BULK_EXIT_MS = 120

/** --duration-exit */
export const PANEL_EXIT_MS = 120

/** --duration-settle */
export const STAGE_CROSSFADE_MS = 200
